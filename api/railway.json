{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @readyyet/db run generate && pnpm --filter @readyyet/shared run build && pnpm --filter @readyyet/api run build",
    "watchPatterns": ["/api/**", "/packages/**", "/package.json", "/pnpm-lock.yaml", "/pnpm-workspace.yaml", "/.nvmrc"]
  },
  "deploy": {
    "preDeployCommand": ["pnpm --filter @readyyet/db run migrate:deploy && pnpm --filter @readyyet/db run seed"],
    "startCommand": "node --enable-source-maps api/dist/main.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
